name: Build

on:
  workflow_dispatch:
    inputs:
      electron:
        description: 'Electron version to build'
        required: true
        default: '32'
        type: choice
        options:
        - 32
        - 33
        - 34
        - 35
        - 36
        - 37
  workflow_call:
    inputs:
      electron:
        description: 'Electron version to build'
        required: true
        type: string

env:
  DOTNET_CLI_TELEMETRY_OPTOUT: 1 
  DOTNET_NOLOGO: 1

jobs:
  electron-version:
    runs-on: macos-latest
    outputs:
      test-version: ${{ steps.electron-test-version.outputs.test-version }}

    name: electron-version
    steps:

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup NodeJS
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Cache node modules
        id: cache-nodemodules
        uses: actions/cache@v4
        env:
          cache-name: cache-node-modules
        with:
          path: node_modules
          key: ${{ runner.os }}-${{ hashFiles('package.json') }}
 
      - name: npm install
        shell: bash
        run: npm i
        env:
          DOTNET_CLI_TELEMETRY_OPTOUT: 1

      - name: Get latest Electron version for ${{ inputs.electron }}
        id: electron-test-version
        shell: bash
        run: |
          node tools/getVersion.js ${{ inputs.electron }}
          echo "test-version=$(cat electron.txt)" >> $GITHUB_OUTPUT

  build-win:
    runs-on: windows-2025
    environment: electron-edge-js
    strategy:
      fail-fast: true
       
    name: build electron-${{ inputs.electron }} windows-2025
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Electron ${{ inputs.electron }}
        uses: ./.github/workflows/build-electron.yml
        with:
          electron: ${{ inputs.electron }}
          os: windows-2025

  build-darwin-x64:
    runs-on: macos-13
    environment: electron-edge-js
    strategy:
      fail-fast: true
       
    name: build electron-${{ inputs.electron }} macos-13
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Electron ${{ inputs.electron }}
        uses: ./.github/workflows/build-electron.yml
        with:
          electron: ${{ inputs.electron }}
          os: macos-13

  build-darwin-arm64:
    runs-on: macos-15
    environment: electron-edge-js
    strategy:
      fail-fast: true
       
    name: build electron-${{ inputs.electron }} macos-15
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Electron ${{ inputs.electron }}
        uses: ./.github/workflows/build-electron.yml
        with:
          electron: ${{ inputs.electron }}
          os: macos-15

  test:
    runs-on: ${{ matrix.os }}
    needs: [electron-version, build-win, build-darwin-x64, build-darwin-arm64]
    strategy:
      matrix:
        # os: [windows-2022]
        os: [windows-2025, windows-11-arm, macos-13, macos-15]
      fail-fast: false

    name: test electron-${{ needs.electron-version.outputs.test-version }} ${{ matrix.os }}
    steps:

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Test build
        uses: ./.github/actions/test-build
        timeout-minutes: 10
        with:
          electron: ${{ needs.build.outputs.test-version }}
          os: ${{ matrix.os }}
  
  upload-artifacts:
    runs-on: ubuntu-latest
    needs: test
    strategy:
      fail-fast: false

    name: upload artifacts electron-${{ inputs.electron }}
    steps:

      - name: Download artifacts
        uses: actions/download-artifact@v4
        if: success()
        with:
          path: release
          pattern: win32-electron-edge-js*

      - name: Download artifacts
        uses: actions/download-artifact@v4
        if: success()
        with:
          path: release
          pattern: darwin-electron-edge-js*

      - name: Upload artifacts
        uses: actions/upload-artifact/merge@v4
        if: success()
        with:
          name: electron-edge-js-${{ inputs.electron }}
    